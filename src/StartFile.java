
public class StartFile {
	 
	 String encoding;    //The character encoding of the custom start file, corresponding to either the content element's encoding attribute (if any), or the default encoding
	 String contentType; //The media type of the start file, corresponding to the content element's type attribute or to a media type derived from the default start files table.


}
